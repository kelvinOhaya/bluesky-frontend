Next Steps:
- Update the join and leaving group logic to be more compatible and clean (separate events, aggretation, etc). start with joinRoom

-make it so membercount updates when data is sent to the frontend foundUser

- Adjust backend starting with these issues to emit socket events from the server for chatRoom events instead of send-return-send again-return again

production:
- Fix an issue where sockets don't work



    
