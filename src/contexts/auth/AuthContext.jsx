import { createContext } from "react";

//apparently you can't export multiple things in react, so this is split into three files instead of two
const AuthContext = createContext();
export default AuthContext;
